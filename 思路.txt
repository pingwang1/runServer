1.目标：在输入框输入Python代码，点击运行，右边显示代码结果。

2.步骤：
	那首先我们需要一个页面来显示我们的输入输出，还有一个按钮；
	然后就是通过点击按钮事件获取输入的内容；（用到了Ajax与服务器交互；它会自动封装请求行，请求头，空格）
	再就是发生内容到服务器；
	服务器接收内容；
	服务器处理内容；（用正则得到我们的代码块写入新的文件命名为.py的文件，
		然后用subprocess模块下的方法去获取新文件执行后的结果）
	服务器返回结果，包括：响应行、响应头、空格、响应体（代码执行结果）；
	最后就是页面接收到返回结果，并显示到输出框。

3.遇到的问题：主要就是在处理服务器接收页面发送过来的数据，用正则拆分，这块坑比较多。


测试：
class Person(object):
    def __init__(self,name):
        self.name = name
    def __str__(self):
        return "姓名是:%s"%self.name
p = Person("嘻嘻")
print(p.name)
print(p)